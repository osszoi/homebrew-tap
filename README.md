# osszoi/homebrew-tap

Homebrew formulae for [reap](https://github.com/osszoi/reap).

## Usage

```sh
brew install osszoi/tap/reap
```

Formulae here are published automatically by [dist](https://github.com/axodotdev/cargo-dist) on each `reap` release.
